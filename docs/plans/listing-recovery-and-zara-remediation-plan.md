# Plan: Listing Recovery and Zara Remediation

**Created:** 2026-04-21
**Agent:** Codex
**Status:** DONE
**Touches buckets:** 3. Acquisition + Browser Runtime, 4. Extraction, 5. Publish + Persistence

## Goal

Fix the concrete regressions shown in the saved acceptance artifacts and Zara repros without adding another recovery layer. Done means listing crawls stop returning garbage chrome/category rows, acquisition stops giving up after weak or zero-card probes when product evidence is present, and Zara detail extraction stops leaking duplicate option axes / page-context-style filler while restoring real variant data. Net LOC for the implementation slices should be negative by deleting duplicate or no-op logic as part of the fix.

## Acceptance Criteria

- [ ] Zara listing returns 12 real product rows for the current failing crawl path, not 5 structured rows and not polluted category-cloud/navigation rows.
- [ ] Zara listing records contain product-detail URLs/titles only; SEO cloud, utility links, and menu labels are excluded upstream.
- [ ] Acquisition/traversal does not stop on Zara-like listing pages with `listing_card_count=0` when the DOM contains real product-grid cards; bounded recovery only runs when evidence supports it.
- [ ] Zara detail extraction does not emit duplicate variant axes or copy-SKU values inside option fields, and `variants` / `selected_variant` remain present in JSON and CSV-safe record data.
- [ ] No `page_context`-style filler field leaks into `record.data` or JSON/CSV export payloads for the Zara detail path.
- [ ] The 2026-04-22 full-pipeline acceptance artifact (`backend/artifacts/test_sites_acceptance/20260422T040347Z__full_pipeline__test_sites_tail.json`) is the primary gate for this pass, not the acquire-only or extraction-smoke runners.
- [ ] `ecommerce_listing` records no longer persist `additional_images`; listing outputs stay limited to required/high-signal fields until detail quality is stable.
- [ ] Full-pipeline thin passes with bad or missing core commerce fields (`LC02 Discogs`, `LC11 Rockler`, `DC11 iFixit`, `DC20 BackMarket`, and any equivalent repros) either recover trustworthy `price` / variant-like data upstream or stop counting as clean success.
- [ ] Non-Shopify commerce-detail fixtures recover meaningful variant fields (`variant_axes`, `variant_count`, `selected_variant`) through shared DOM / JS-state / payload owners rather than Shopify-only happy paths.
- [ ] Stale or dead URLs are ignored by the quality gate: they must be refreshed, quarantined, or explicitly classified as stale instead of dragging extraction remediation toward dead pages.
- [ ] Net code delta across touched implementation files is <= 0 LOC.
- [ ] `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_traversal_runtime.py tests/services/test_selectolax_css_migration.py tests/services/test_detail_extractor_structured_sources.py tests/services/test_record_export_service.py -q`

## Do Not Touch

- `backend/app/api/*` — no route contract churn.
- `backend/app/services/publish/verdict.py` — do not paper over upstream extraction/acquisition failures.
- `backend/app/services/pipeline/persistence.py` — no downstream record cleanup hacks.
- `frontend/*` — leave the UI alone unless a backend contract bug makes that unavoidable.
- `backend/app/services/adapters/*` — only touch if the owning failure is proven to be adapter-specific; Zara should be fixed in generic/runtime owners first.

## Slices

### Slice 1: Lock Regressions to Saved Zara and Acceptance Evidence
**Status:** DONE
**Files:** `backend/tests/services/test_traversal_runtime.py`, `backend/tests/services/test_selectolax_css_migration.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`, `backend/tests/services/test_record_export_service.py`
**What:** Add focused regressions from the saved Zara listing/detail artifacts and the acceptance findings so later fixes are forced to hit the real failures instead of synthetic happy paths.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_traversal_runtime.py tests/services/test_selectolax_css_migration.py tests/services/test_detail_extractor_structured_sources.py tests/services/test_record_export_service.py -q`

### Slice 2: Make Listing Card Detection Match Actual Zara Product Tiles
**Status:** DONE
**Files:** `backend/app/services/config/selectors.exports.json`, `backend/app/services/acquisition/traversal.py`, `backend/app/services/acquisition/browser_readiness.py`, `backend/tests/services/test_traversal_runtime.py`
**What:** Use the existing selector/config owners so readiness and traversal count the same product-card shapes that extraction already sees on Zara-like grids; delete or collapse any duplicate no-op card-count paths that keep recovery from progressing.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_traversal_runtime.py tests/services/test_browser_expansion_runtime.py -q`

### Slice 3: Remove Garbage Listing Candidates Upstream
**Status:** DONE
**Files:** `backend/app/services/listing_extractor.py`, `backend/app/services/extract/listing_candidate_ranking.py`, `backend/tests/services/test_selectolax_css_migration.py`
**What:** Tighten the listing-card and rendered-card filters so taxonomy clouds, utility links, promo rails, and menu items cannot out-rank real product rows; prefer one clean candidate-set path instead of overlapping DOM/rendered heuristics fighting each other.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_selectolax_css_migration.py tests/services/test_crawl_engine.py -q`

### Slice 4: Fix Zara Detail Variant Parsing and Export Hygiene
**Status:** DONE
**Files:** `backend/app/services/detail_extractor.py`, `backend/app/services/record_export_service.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`, `backend/tests/services/test_record_export_service.py`
**What:** Deduplicate same-axis DOM variant groups, ignore copy-code/button noise, keep real `variants` / `selected_variant` data intact, and ensure JSON/CSV exports stay limited to user-facing fields rather than raw markdown/page-context filler.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_record_export_service.py -q`

### Slice 5: Tighten Browser Give-Up Rules Without Growing Recovery Layers
**Status:** DONE
**Files:** `backend/app/services/acquisition/browser_page_flow.py`, `backend/app/services/acquisition/browser_runtime.py`, `backend/app/services/acquisition/traversal.py`, `backend/tests/services/test_browser_expansion_runtime.py`, `backend/tests/services/test_pipeline_core.py`
**What:** Audit the old-app recovery port against the current call path, remove inert branches, and keep only one bounded retry/wait path with diagnosable stop reasons when browser acquisition would otherwise give up too early.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_browser_expansion_runtime.py tests/services/test_pipeline_core.py -q`

### Slice 6: Harden Commerce Requested-Field and Variant Coverage
**Status:** DONE
**Files:** `backend/app/services/detail_extractor.py`, `backend/app/services/network_payload_mapper.py`, `backend/tests/services/test_crawl_engine.py`, `backend/tests/services/test_network_payload_mapper.py`
**What:** Keep batch `requested_fields` / "Additional details" input live through batch fan-out, stop ghost-routed listing APIs from seeding false detail records, and prevent site-shell Open Graph fallbacks from persisting stale SPA detail pages as fake product successes while preserving real variant-rich detail output.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_network_payload_mapper.py tests/services/test_crawl_engine.py tests/services/test_detail_extractor_structured_sources.py tests/services/test_detail_extractor_priority_and_selector_self_heal.py -q && .\.venv\Scripts\python.exe -m pytest tests -q`

### Slice 7: Unblackbox Additional Fields and Requested DOM Sections
**Status:** DONE
**Files:** `frontend/components/crawl/shared.tsx`, `frontend/components/crawl/crawl-config-screen.test.ts`, `backend/app/services/detail_extractor.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`
**What:** Align UI additional-field normalization with backend requested-field normalization so punctuation-heavy labels such as "Features & Benefits" survive dispatch, and keep ecommerce-detail DOM completion alive when requested custom section fields such as `product story` are visibly advertised by the page instead of letting structured-data early exit hide them.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_crawl_engine.py tests/services/test_browser_expansion_runtime.py -q` and `cd frontend; npm test -- components/crawl/crawl-config-screen.test.ts`

### Slice 8: Variant Recovery Becomes Quality-Gated, Not Presence-Gated
**Status:** DONE
**Files:** `backend/app/services/detail_extractor.py`, `backend/app/services/extract/detail_tiers.py`, `backend/app/services/field_value_candidates.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`, `backend/tests/services/test_shared_variant_logic.py`
**What:** Keep the current multi-tier candidate architecture, but stop letting any non-empty JS-state `variants` payload suppress DOM recovery. DOM variant extraction now runs when existing variant data is weak, and weak rows without usable `option_values` no longer survive once stronger DOM-backed variants exist.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_shared_variant_logic.py tests/services/test_crawl_engine.py -q --basetemp=.pytest-tmp-codex-1`

### Slice 9: Image Resolution Merges Better Sources Without Per-Tier Drift
**Status:** DONE
**Files:** `backend/app/services/detail_extractor.py`, `backend/app/services/field_value_dom.py`, `backend/app/services/js_state_mapper.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`, `backend/tests/services/test_field_value_dom.py`
**What:** Delete the split JS-state-vs-DOM image winner logic by reusing one canonical image dedupe/filter path. `image_url` now materializes from the best canonical image candidate across sources, `additional_images` merges and dedupes across sources, and obvious provider/payment/logo noise is filtered once in the shared image owner.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_browser_expansion_runtime.py tests/services/test_field_value_dom.py -q --basetemp=.pytest-tmp-codex-6`

### Slice 10: Browser Extractability and DOM Completion Share One Contract
**Status:** DONE
**Files:** `backend/app/services/acquisition/browser_page_flow.py`, `backend/app/services/detail_extractor.py`, `backend/app/services/field_value_dom.py`, `backend/tests/services/test_browser_expansion_runtime.py`
**What:** Remove the heading-only browser extractability heuristic. Browser-time expansion skip decisions and extraction-time DOM completion checks now share the same requested-content extractability contract, including exact labels, normalized aliases, DOM sections, DOM patterns, and selector-backed fields.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_browser_expansion_runtime.py tests/services/test_detail_extractor_structured_sources.py -q`

### Slice 11: Preserve Exact Requested Labels Through Batch Fan-Out
**Status:** DONE
**Files:** `backend/app/services/pipeline/core.py`, `backend/tests/services/test_batch_runtime.py`, `backend/tests/services/test_crawl_service.py`, `backend/tests/services/test_field_policy.py`
**What:** Keep raw requested labels preserved through batch per-URL fan-out and extraction dispatch instead of canonicalizing them before acquisition. Canonical normalization still happens where matching logic needs it, but exact labels such as `Features & Benefits` now survive into acquisition/extraction/runtime shaping without adding a new normalization subsystem.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_field_policy.py tests/services/test_crawl_service.py tests/services/test_batch_runtime.py -q --basetemp=.pytest-tmp-codex-3`

### Slice 12: Lock Commerce Smoke Failures To Common Regression Fixtures
**Status:** DONE
**Files:** `backend/tests/services/test_crawl_fetch_runtime.py`, `backend/tests/services/test_crawl_engine.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`, `backend/tests/test_harness_support.py`, `backend/run_extraction_smoke.py`
**What:** Re-baseline the current plan around the verified full-pipeline artifact and keep the smoke runners diagnostic-only. Add focused guards for the real persisted regressions surfaced on 2026-04-22: listing `additional_images`, thin-pass price failures, and missing variants. Only keep smoke-runner follow-up where the same bug reproduces on the production pipeline or clearly points at an upstream acquisition owner.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_crawl_fetch_runtime.py tests/services/test_crawl_engine.py tests/services/test_detail_extractor_structured_sources.py tests/test_harness_support.py -q`

### Slice 13: Remove Listing Additional Images And Fix Price Thinness Upstream
**Status:** DONE
**Files:** `backend/app/services/config/field_mappings.py`, `backend/app/services/listing_extractor.py`, `backend/app/services/field_policy.py`, `backend/tests/services/test_crawl_engine.py`, `backend/tests/services/test_selectolax_css_migration.py`
**What:** Remove `additional_images` from the listing surface and tighten listing extraction around required fields instead of gallery completeness. In the same pass, fix the verified thin-price cases in the owning extraction paths so title-only or obviously wrong-price listing/detail records stop passing as acceptable commerce output.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_crawl_engine.py tests/services/test_selectolax_css_migration.py tests/services/test_field_policy.py -q`

### Slice 14: Recover Generic Variant Coverage Beyond Shopify-Style Payloads
**Status:** DONE
**Files:** `backend/app/services/detail_extractor.py`, `backend/app/services/structured_sources.py`, `backend/app/services/js_state_mapper.py`, `backend/app/services/network_payload_mapper.py`, `backend/app/services/extract/shared_variant_logic.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`, `backend/tests/services/test_shared_variant_logic.py`, `backend/tests/services/test_state_mappers.py`
**What:** Expand commerce-detail variant recovery in the existing shared owners so non-Shopify PDPs can surface deterministic size/color/grade/compatibility-style options from DOM controls, JS state, and network payloads without unnamed-axis noise. This slice owns the verified missing-variant issue from the full pipeline.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_shared_variant_logic.py tests/services/test_state_mappers.py tests/services/test_crawl_engine.py -q`

### Slice 15: Ignore Stale URLs And Defer Minor Listing URL Pollution
**Status:** DONE
**Files:** `backend/harness_support.py`, `backend/tests/test_harness_support.py`, `backend/corpora/commerce_25.json`, `backend/tests/services/test_crawl_engine.py`
**What:** Treat stale or dead URLs as corpus hygiene, not extraction failures. Refresh or quarantine dead fixtures and classify them explicitly so they do not distort remediation priorities. Minor listing-URL pollution remains intentionally low priority in this pass unless it materially blocks required-field recovery or creates large-scale noise.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/test_harness_support.py tests/services/test_crawl_engine.py -q`

### Slice 16: Fix Saleor Detail Long-Text and Image Hygiene
**Status:** DONE
**Files:** `backend/app/services/detail_extractor.py`, `backend/app/services/field_value_dom.py`, `backend/app/services/field_value_core.py`, `backend/app/services/field_policy.py`, `backend/app/services/config/field_mappings.exports.json`, `backend/tests/services/test_detail_extractor_structured_sources.py`
**What:** Keep ecommerce-detail DOM section bodies alive when they contain materially richer description text than thin structured blurbs, stop collapsing `Product Details` into `specifications`, and dedupe Next.js-style image proxy URLs so transformed copies of the hero asset do not survive as fake `additional_images`.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_field_value_dom.py tests/services/test_crawl_engine.py -q`

### Slice 17: Remove Low-Signal Detail Handle Output And Tab-Rail Description Pollution
**Status:** DONE
**Files:** `backend/app/services/detail_extractor.py`, `backend/app/services/field_value_dom.py`, `backend/app/services/config/field_mappings.exports.json`, `backend/tests/services/test_detail_extractor_structured_sources.py`, `backend/tests/services/test_crawl_engine.py`
**What:** Keep platform slug/handle fields out of the default ecommerce-detail schema and reject very short tab/button-only DOM section text before it can override real product descriptions or specifications on Shopify-like PDPs such as Boat.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_crawl_engine.py -q`

### Slice 17: Fix Generic Listing-Card Misses and Synthetic Section Fields
**Status:** DONE
**Files:** `backend/app/services/acquisition/traversal.py`, `backend/app/services/config/selectors.exports.json`, `backend/app/services/field_value_dom.py`, `backend/tests/services/test_traversal_runtime.py`, `backend/tests/services/test_field_value_dom.py`, `backend/tests/services/test_browser_expansion_runtime.py`
**What:** Keep new commerce listings from repeating the same zero-card/browser-diagnostic failure class when configured selectors miss a valid product grid. Listing-card counting now falls back to the shared heuristic after a selector miss, the generic selector set matches camelCase/lowercase `productCard` classnames, and requested-content extractability only maps canonical or explicitly requested section labels instead of turning arbitrary product headings into fake field names.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_traversal_runtime.py tests/services/test_field_value_dom.py tests/services/test_browser_expansion_runtime.py -q && .\.venv\Scripts\python.exe -m pytest tests -q`

### Slice 18: Harden Listing Readiness, Original-DOM Recovery, And Run-Identity Idempotence
**Status:** DONE
**Files:** `backend/app/services/acquisition/browser_readiness.py`, `backend/app/services/acquisition/browser_page_flow.py`, `backend/app/services/listing_extractor.py`, `backend/app/services/config/surface_hints.py`, `backend/app/services/field_value_core.py`, `backend/app/services/pipeline/core.py`, `backend/app/services/pipeline/persistence.py`, `backend/tests/services/test_browser_expansion_runtime.py`, `backend/tests/services/test_crawl_engine.py`, `backend/tests/services/test_pipeline_core.py`, `docs/BUSINESS_LOGIC.md`, `docs/backend-architecture.md`, `AGENTS.md`
**What:** Fix the shared pipeline decisions behind three live failure classes. Listing browser readiness no longer treats thin shell text plus generic structured data as sufficient evidence on listing surfaces, listing extraction can retry the original DOM when cleanup strips header-nested product links, generic ecommerce detail-path hints now recognize `/proddetail/` and same-site cross-subdomain detail links, pipeline logs distinguish a real adapter from the generic extraction path, and persistence skips already-inserted `(run_id, url_identity_key)` rows so reruns do not crash on duplicate-key inserts.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_browser_expansion_runtime.py tests/services/test_crawl_engine.py tests/services/test_pipeline_core.py -q`

### Slice 19: Review Comment Follow-Up Hardening
**Status:** DONE
**Files:** `backend/app/core/telemetry.py`, `backend/app/services/acquisition/browser_identity.py`, `backend/app/services/acquisition/traversal.py`, `backend/tests/services/test_browser_context.py`, `backend/tests/services/test_traversal_runtime.py`
**What:** Validate the new review comments against the live code path and fix only the real robustness issues. Telemetry now initializes the optional `structlog` module reference eagerly, browser identity normalization ignores mapping-like settings and accepts tuple-like brand entries when rebuilding coherent client hints, and the listing-card heuristic fallback now counts leaf card candidates from rendered HTML instead of broad support/detail sections.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_browser_context.py tests/services/test_traversal_runtime.py -q && .\.venv\Scripts\python.exe -m pytest tests/services/test_crawl_fetch_runtime.py -q -k "create_browser_identity_builds_generator_lazily or read_network_payload_body_maps_read_timeouts_to_timeout"`

### Slice 21: Restore Domain-Memory Selector Reloads And Fix Adjacent Detail Sections
**Status:** DONE
**Files:** `backend/app/api/selectors.py`, `backend/app/services/selectors_runtime.py`, `backend/app/services/field_value_dom.py`, `backend/tests/services/test_selectors_api.py`, `backend/tests/services/test_selector_pipeline_integration.py`, `backend/tests/services/test_field_value_dom.py`, `backend/tests/services/test_detail_extractor_structured_sources.py`
**What:** Bring the selector API back in line with the documented domain-memory contract so `GET /api/selectors?domain=...` returns all saved selector rows for that domain when `surface` is omitted, which lets Crawl Studio and Domain Memory reload/update surface-specific selectors correctly. In the same pass, keep adjacent detail headings bound to their own bodies so TradeIndia-style `Product Specifications` and `Company Details` blocks stop swapping labels and custom requested fields such as `company_details` recover the actual long-form company content.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_selectors_api.py tests/services/test_selector_pipeline_integration.py tests/services/test_field_value_dom.py tests/services/test_detail_extractor_structured_sources.py -q`

### Slice 22: Fix Mixed Table-Row Listing Anchors On New Commerce Sites
**Status:** DONE
**Files:** `backend/app/services/listing_extractor.py`, `backend/tests/services/test_selectolax_css_migration.py`
**What:** Keep mixed table-row commerce listings such as BrickLink in the shared listing owner instead of adding another site branch. Structural-link filtering now classifies obvious utility/category terminals from the URL tail instead of rejecting any deep detail path that merely contains a non-listing token earlier in the path, fallback title selection now considers prominent non-anchor row titles, and primary-anchor ranking now uses the detected title position so breadcrumb links that only appear after the row title do not outrank the actual detail link.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_selectolax_css_migration.py tests/services/test_crawl_engine.py -q && .\.venv\Scripts\python.exe -m pytest tests -q`

### Slice 23: Reject Job-Hub Listing Noise Before It Outranks Real Job Rows
**Status:** DONE
**Files:** `backend/app/services/listing_extractor.py`, `backend/tests/services/test_crawl_engine.py`, `docs/BUSINESS_LOGIC.md`
**What:** Keep Instahyre-style job listings in the shared listing owner instead of patching around them in routing or persistence. Job listing support now rejects search/city hub links and cross-surface product/academy links before rendered artifacts can outrank real structured job postings, and job detail-path checks no longer inherit ecommerce product markers on job surfaces.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_crawl_engine.py -q -k "prefers_rendered_listing_cards_over_thin_structured_records or rejects_job_listing_hub_links_when_structured_job_rows_exist"`

### Slice 24: Restore Browser Session Continuity And Strip Tracked Detail URLs
**Status:** DONE
**Files:** `backend/app/services/acquisition/cookie_store.py`, `backend/app/services/acquisition/browser_runtime.py`, `backend/app/services/crawl_fetch_runtime.py`, `backend/app/services/crawl_utils.py`, `backend/app/services/field_value_core.py`, `backend/tests/services/test_browser_context.py`, `backend/tests/services/test_field_value_core.py`, `backend/tests/services/test_acquirer.py`, `docs/backend-architecture.md`, `docs/CODEBASE_MAP.md`
**What:** Restore the missing cookie-store behavior in the acquisition owner instead of adding another anti-bot workaround. Browser contexts now reuse per-run Playwright storage state so listing-to-detail follow-ups keep cookies/localStorage/consent state, and tracked/signed detail URLs now strip low-signal click/search context params upstream while preserving functional params such as `variant` or `q`.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_browser_context.py tests/services/test_field_value_core.py tests/services/test_acquirer.py -q`

### Slice 25: Remember Vendor-Blocked Hosts And Prefer Browser-First
**Status:** DONE
**Files:** `backend/app/services/acquisition/pacing.py`, `backend/app/services/crawl_fetch_runtime.py`, `backend/tests/services/test_crawl_fetch_runtime.py`, `backend/tests/services/test_pacing.py`, `docs/backend-architecture.md`
**What:** Keep recent vendor-block memory in the existing pacing owner so the next fetch to the same host can skip the initial `curl` probe and go browser-first for the cache TTL window. This closes the remaining generic anti-bot gap behind the Wellfound-style failure class, where a confirmed DataDome block was still being replayed as another `curl -> browser` sequence on the same host.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_crawl_fetch_runtime.py tests/services/test_pacing.py -q`

### Slice 26: Repair Client Hint And HTTP Header Coherence
**Status:** DONE
**Files:** `backend/app/services/acquisition/browser_identity.py`, `backend/app/services/network_resolution.py`, `backend/tests/services/test_browser_context.py`, `backend/tests/services/test_crawl_fetch_runtime.py`, `docs/backend-architecture.md`
**What:** Keep browser/header identity repair in the existing acquisition/network owners instead of bolting on more challenge retries. Playwright contexts now replace malformed or version-incoherent `sec-ch-ua*` headers with deterministic Chrome client hints derived from the selected UA, and the shared HTTP default headers now advertise the same Chrome client-hint family plus `Upgrade-Insecure-Requests` when the configured UA is Chrome-like. Live verification moved the Mouser PDP repro from an immediate `403` challenge to a softer `200` challenge page, proving the coherence fix is engaged even though Wellfound and Etsy still block first-hit browser traffic on the current egress.
**Verify:** `cd backend; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest tests/services/test_browser_context.py tests/services/test_crawl_fetch_runtime.py -q`

## Doc Updates Required

- [x] `docs/backend-architecture.md` — update acquisition/listing behavior if owners or recovery semantics materially change.
- [x] `docs/CODEBASE_MAP.md` — `acquisition/cookie_store.py` now owns per-run storage-state persistence instead of a no-op policy stub.
- [ ] `docs/INVARIANTS.md` — update only if a runtime contract changes.
- [ ] `docs/ENGINEERING_STRATEGY.md` — add a new anti-pattern only if this work uncovers another duplicate-recovery pattern worth codifying.

## Notes

- 2026-04-22 slice-24 stayed in the acquisition/input-normalization owners instead of adding another provider-specific bypass: `acquisition/cookie_store.py` now persists merged per-run Playwright storage state under `cookie_store_dir`, `browser_runtime.py` reloads that state into later contexts in the same run, and `reset_fetch_runtime_state()` clears the in-memory store so dashboard/test resets do not reuse stale state. In the same pass, tracking cleanup was widened upstream so extracted and user-entered commerce/job URLs drop signed click/search context params such as `click_*`, `content_source`, `pf_from`, `sr_prefetch`, and Mouser-style `qs` while preserving functional params like `variant`, `q`, and `id`. Focused verification passed with `pytest tests/services/test_browser_context.py tests/services/test_field_value_core.py tests/services/test_acquirer.py -q`; the motivating live repros were Etsy tracked detail links and the same replay-to-challenge failure mode on Mouser detail URLs.
- 2026-04-22 slice-12/13 implementation stayed in the owning extraction/config modules: focused regressions now lock listing `additional_images` removal and shell-price pollution on detail pages, `ecommerce_listing` no longer exposes `additional_images` in the canonical schema, the generic listing owner stops seeding `additional_images`, and detail DOM fallback no longer infers `price` from whole-page chrome text. Focused verification passed with `pytest tests/services/test_crawl_engine.py -q`, `pytest tests/services/test_selectolax_css_migration.py -q`, and `pytest tests/services/test_field_policy.py -q`.
- 2026-04-22 slice-16 implementation stayed in the existing detail/image owners: ecommerce-detail long-text fields now rank `dom_sections` ahead of thin structured blurbs when a real accordion/section body is present, `product_details` is a distinct canonical ecommerce-detail field instead of an alias for `specifications`, sibling section capture no longer truncates the second paragraph/list block on Saleor-style pages, and image dedupe unwraps Next.js `/_next/image?...` proxies back to the underlying asset so hero-image transforms do not leak into `additional_images`. Focused verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py -q`, `pytest tests/services/test_field_value_dom.py -q`, and `pytest tests/services/test_crawl_engine.py -q`.
- 2026-04-22 slice-17 implementation stayed in the existing extraction owners as well: the default ecommerce-detail schema no longer includes low-signal platform slug output like `handle`, and long-text candidate intake now rejects very short non-prose `dom_sections` text so tab rails like `Description | specifications | Reviews (192)` cannot override a real product description on Boat-style pages. Focused verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_crawl_engine.py -q`, and the live Boat repro now returns the meta/product description instead of the tab labels.
- 2026-04-22 slice-17 implementation stayed in the shared acquisition/DOM owners instead of adding another site branch: `count_listing_cards()` now falls back to the generic heuristic when configured selectors return zero, the shared ecommerce card-selector config now matches case-variant `productCard` classnames, and `requested_content_extractability()` no longer turns arbitrary section headings into synthetic field names unless the label is canonical or explicitly requested. Focused verification passed with `pytest tests/services/test_traversal_runtime.py -q`, `pytest tests/services/test_field_value_dom.py -q`, and `pytest tests/services/test_browser_expansion_runtime.py -q`; a live Thesouledstore listing repro moved from `listing_card_count=0` / `detail_expansion.reason=requested_content_already_extractable` to `listing_card_count=24` / `detail_expansion.reason=non_detail_surface`. Full `pytest tests -q` still hits the pre-existing unrelated LOC-budget failure in `tests/services/test_structure.py` for `app/services/detail_extractor.py` (1130 lines).
- 2026-04-22 slice-14 implementation stayed in the shared JS-state owner instead of adding adapter bloat: `js_state_mapper.py` now prefers richer nested product payloads over shallow landing nodes and recognizes direct non-Shopify variant axes such as `condition`, `grade`, `storage`, and `memory` when variants expose them as first-class keys. Focused verification passed with `pytest tests/services/test_state_mappers.py tests/services/test_shared_variant_logic.py tests/services/test_detail_extractor_structured_sources.py tests/services/test_crawl_engine.py -q`.
- 2026-04-22 slice-23 stayed in the shared listing owner instead of touching inference or publish paths: `listing_extractor.py` now rejects job city/search hubs such as `jobs in Bangalore` plus cross-surface `/product/` academy links before rendered artifacts can outrank real job rows, and job detail-path checks no longer borrow ecommerce product markers on job surfaces. Focused verification passed with `pytest tests/services/test_crawl_engine.py -q -k "prefers_rendered_listing_cards_over_thin_structured_records or rejects_job_listing_hub_links_when_structured_job_rows_exist"`; live Instahyre repros moved from rendered hub-link noise back to structured job rows on successful browser acquisitions.
- 2026-04-22 trust boundary verification:
  - `backend/run_acquire_smoke.py` is acquisition-only and does not run persistence or the full extraction/verdict path.
  - `backend/run_test_sites_acceptance.py --mode full_pipeline` runs `harness_support.run_site_harness()` → `process_run()` and reads persisted `CrawlRecord` data back out of the database, so this is the primary artifact for plan priority.
- 2026-04-22 verified full-pipeline issues from the saved artifact and persisted records:
  - only two hard failures remain in the current acceptance artifact: `LC08 AutoZone` (`blocked`) and `LC19 GovPlanet` (`listing_detection_failed`).
  - thin-pass records are still real bugs, not acceptable wins: `Discogs` run `8` persists only `source_url/title/url` with title `Make Offer / Details`; `Rockler` run `11` persists only `source_url/title/url`; `iFixit` run `15` persists price `1`; `BackMarket` run `17` persists title `iPhone` and price `20`.
  - listing `additional_images` is still live in persisted `ecommerce_listing` records; e.g. acceptance run `18` (`31philliplim`) stores `additional_images` alongside listing rows.
  - variant fields are absent in the audited thin-pass runs and remain a verified cross-site gap outside the Shopify-style path.
- 2026-04-22 commerce smoke baseline (`backend/artifacts/extraction_smoke/20260422T035152Z__sandbox_listing-sandbox_detail-real_listing-real_detail-extended_listing.json`): `17 PASS / 8 FAIL`. The failures cluster into shared owners rather than random one-offs:
  - JS-dependent listing shells: `S20 scrapingcourse`, `LC01 Reverb`, `CE01 Zadig&Voltaire`, `CE08 Todd Snyder`.
  - Stale or 404 detail/listing URLs: `DC12 Vitacost`, `DC17 Rockler`, `LC06 Uline`.
  - Explicit anti-bot block: `LC10 Chewy` (`429`, Akamai challenge) stays out of scope for generic extraction fixes.
  - Low-priority partial coverage: `LC14 AbeBooks` has `price` coverage `0.7`; treat as follow-up only after the shared regression classes are fixed.
- 2026-04-22 Gemini audit hardening stayed in the existing acquisition/platform owners: browser network payload reads now use a config-backed timeout and map timed-out body buffering to the existing `too_large` outcome, while platform-family HTML regex detection now truncates HTML to a config-backed search window before synchronous regex evaluation on the fetch hot path. Focused verification passed with `pytest tests/services/test_crawl_fetch_runtime.py -q -k "read_network_payload_body or should_capture_network_payload"`, `pytest tests/services/test_platform_detection.py -q`, and `pytest tests/services/test_browser_expansion_runtime.py -q -k browser_capture`.
- Concrete evidence for this plan comes from `docs/audits/SMOKETEST.md`, `backend/artifacts/test_sites_acceptance/20260421T094742Z__full_pipeline__test_sites_tail.json`, the saved Zara artifacts under `backend/artifacts/runs/1` and `backend/artifacts/runs/2`, and the live Zara repro run during this session.
- 2026-04-22 slice-21 stayed in the existing selector/runtime and DOM-section owners: `GET /api/selectors?domain=...` now returns every selector row stored for that domain when `surface` is omitted, which restores Crawl Studio/domain-memory reloads for saved surface-specific selectors instead of only showing `generic` rows, and adjacent detail headings now prefer their own sibling body content when a wrapped-content probe only finds another heading label. Focused verification passed with `pytest tests/services/test_selectors_api.py tests/services/test_selector_pipeline_integration.py tests/services/test_field_value_dom.py tests/services/test_detail_extractor_structured_sources.py -q`; the saved TradeIndia/Lyotex artifact now materializes `company_details` from the real company block plus business facts instead of the swapped `Product Specifications` heading text.
- 2026-04-22 full-suite verification after slice-21 passed all functional selector/extraction/runtime tests and failed only the existing LOC-budget guard in `tests/services/test_structure.py` (`app/services/detail_extractor.py`, `app/services/field_value_dom.py`, `app/services/acquisition/browser_page_flow.py`, and `app/services/acquisition/traversal.py` remain over budget). No new behavioral regressions surfaced in the backend suite.
- 2026-04-22 slice-22 stayed in the shared listing owner: a new BrickLink commerce listing repro showed row fragments were already being found, but `_url_is_structural()` rejected the real `/v2/catalog/catalogitem.page?...` detail links because `catalog` appeared in an earlier path segment, `_card_title_node()` preferred the item-number anchor, and `_select_primary_anchor()` then let later breadcrumb links outrank the row detail URL. The fix stayed upstream in `listing_extractor.py`: structural-link filtering now keys off terminal/short-path non-listing tokens, fallback title candidates include prominent non-anchor row titles, and anchor scoring boosts links that appear before a separate title node while demoting breadcrumb links that only appear after it. Focused verification passed with `pytest tests/services/test_selectolax_css_migration.py tests/services/test_crawl_engine.py -q`, a live BrickLink replay now returns 10 clean rows headed by `Small Soccer Set 1 {Kabaya Version}` -> `/v2/catalog/catalogitem.page?S=1428-1`, and the full backend suite still fails only the pre-existing LOC-budget guard in `tests/services/test_structure.py`.
- 2026-04-21 execution update:
  - saved Zara detail artifact now emits a single clean color axis and removes the copied code from option values;
  - saved acceptance artifacts now collapse USAJOBS and Karen Millen false-positive listing rows to zero and promote Startup.jobs from `Bookmark Apply` garbage to job-title rows;
  - live Zara browser acquisition now detects 40 listing cards upstream and extracts 13 product rows from the current listing path, clearing the prior 5-row failure mode;
  - browser-attempt exceptions now carry structured `browser_diagnostics` through batch error handling instead of disappearing as `{}`.
  - phase-2 smoke review found the active diff had already regressed the per-file LOC guard in `listing_extractor.py`; visual-fallback extraction was moved into `extract/listing_visual.py` so the listing owner stays under budget without adding a new recovery layer;
  - extraction runtime now handles XML sitemap/listing payloads as deterministic URL records, removing the 90s timeout path seen on `lafayette148ny.com/media/sitemap-products.xml`;
  - acceptance harness surface inference now recognizes slug-with-id `.../index.html` commerce detail URLs, fixing the `books.toscrape` detail false-positive listing route during acceptance runs;
  - harness `acquisition_only` mode was also repaired during the review pass so prefetch-mode smoke runs no longer die on an unassigned `url_result`;
  - suggestion-driven follow-up fixes stayed inside the existing owners: ecommerce detail titles now reject noisy DOM heading fallbacks and rank structured sources ahead of `dom_h1`, listing title scoring now drops pure-numeric candidates sooner, and generic DOM-variant helpers were pushed back into `extract/shared_variant_logic.py` so `detail_extractor.py` returns under the explicit LOC budget instead of growing another local helper block.
  - follow-up acceptance fixes now drop breadcrumb/list-position title pollution from structured candidates, derive listing/detail titles from meaningful URL slugs only when upstream title candidates are missing or noise, reject rating-only listing titles, and extend harness-only surface inference so `discogs /release/`, detail-like `.htm` product paths, and job-centric root hosts route to the correct extractor while explicit blocked verdicts stay blocked in acceptance reporting.
  - Gemini-audit remediation stayed in the owning buckets: ADP acquisition URL normalization moved into `ADPAdapter`, generic acquisition now resolves adapter-owned normalization through `adapters/registry.py`, detail DOM fallbacks keep linked gallery images, listing extraction rejects numeric-only titles upstream, and browser payload capture rejects oversized declared responses before reading bodies.
  - focused remediation verification passed with `pytest tests/services/test_acquirer.py tests/services/test_job_platform_adapters.py tests/services/test_normalizers.py tests/services/test_detail_extractor_structured_sources.py tests/services/test_crawl_engine.py -q` plus `pytest tests/services/test_crawl_fetch_runtime.py -q -k "read_network_payload_body or should_capture_network_payload"`; a wider subset still has one unrelated pre-existing failure in `tests/services/test_crawl_fetch_runtime.py::test_create_browser_identity_builds_generator_lazily`.
  - slice-5 browser-runtime remediation is now complete: block classification no longer lets Akamai/recaptcha evidence override clearly extractable KitchenAid-class content, no-progress traversal keeps full rendered listing HTML as the primary acquisition payload instead of poisoning extraction with a thin traversal fragment, and Karen Millen-style `next_page_not_found` on an already-populated first page now remains `usable_content` rather than `traversal_failed`;
  - slice-5 verification passed with `pytest tests/services/test_block_detection.py tests/services/test_browser_expansion_runtime.py -q` and the exact slice gate `pytest tests/services/test_browser_expansion_runtime.py tests/services/test_pipeline_core.py -q`; live acquisition repro on 2026-04-21 returned `blocked=false` / `browser_outcome=usable_content` for KitchenAid listing + detail without traversal and for Karen Millen with explicit `paginate` traversal.
  - KitchenAid semantic-expansion follow-up stayed upstream: detail `page_markdown` now prunes review/Q&A/payment containers and low-signal chrome lines, JS-state product detection no longer accepts titled header/payment blobs as product state, JS-state image harvesting rejects payment/logo/bookmark/swatch/video assets, selector-backed long-text fields reject accordion indexes, and long-text candidate intake now drops placeholders like `normal` / `Product summary` before they can win `description` or `specifications`;
  - focused verification for that follow-up passed with `pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_field_value_dom.py tests/services/test_state_mappers.py tests/services/test_browser_expansion_runtime.py -q -k "ignores_review_json_ld_title_description_and_images or ignores_review_qa_controls_and_payment_icons or test_browser_expansion_runtime or test_field_value_dom or test_state_mappers"` plus a live KitchenAid detail repro on 2026-04-21 showing hero-product `image_url`, product-text `description`, null `specifications` instead of the details index, and a materially cleaner `page_markdown` excerpt without the prior review/payment dump.
  - variant follow-up stayed upstream as well: DOM variant intake now only accepts groups that resolve cleanly to canonical `color` / `size` axes, so Q&A-style radiogroups mentioning “size” no longer synthesize bogus variants and the old post-hoc axis/value noise filters were deleted instead of expanded; verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_shared_variant_logic.py tests/services/test_detail_extractor_priority_and_selector_self_heal.py tests/services/test_crawl_engine.py -q`.
  - audit follow-up moved the remaining report-cited tunables out of service bodies and into `app/services/config/*`: detail/listing thresholds now live in `runtime_settings.py`, extraction/browser/variant constants live in `config/extraction_rules.py`, and detail field-set ownership lives in `config/field_mappings.py`; the old skippable Zara copy-code regression was replaced with a committed deterministic fixture in `test_detail_extractor_structured_sources.py`, and full verification passed with `pytest tests -q`.
  - 2026-04-22 review follow-up validated seven new code-review comments against the live diff: three were real and fixed upstream (`browser_detail` ready-detail expansion on ecommerce runs without requested fields, `browser_page_flow` detail markdown label preservation, and `field_value_dom` composition fallback mislabeling); four were confirmed intentional via existing targeted guards (`crawl_crud` raw requested-field persistence, `crawl_fetch_runtime` escalation field propagation, `pipeline/core` canonical LLM missing-field matching, and `pipeline/direct_record_fallback` canonical requested-field scoring inputs). Focused guards passed for the touched owners, and the final backend suite passed with `pytest tests -q`.
  - 2026-04-22 commerce-requested-field follow-up stayed in the extraction owners: batch run creation now persists raw `requested_fields` / `additional_fields` inputs for later per-URL fan-out, ghost-route network payload mapping now rejects multi-record listing envelopes on detail surfaces, and ecommerce detail extraction now drops site-shell/Open-Graph-only false positives instead of persisting fake product records for stale SPA detail routes. Verification passed with `pytest tests/services/test_network_payload_mapper.py tests/services/test_crawl_engine.py tests/services/test_detail_extractor_structured_sources.py tests/services/test_detail_extractor_priority_and_selector_self_heal.py -q` plus `pytest tests -q`; live validation also passed with a 10-site commerce sweep (10/10 on stable listing/detail coverage, including variant-heavy detail extraction and SPA listing output) and a real 3-URL batch detail crawl preserving requested fields across every URL.
  - 2026-04-22 additional-fields follow-up stayed in the owning frontend/extraction modules: UI field normalization now accepts separator-heavy labels like `Features & Benefits` by normalizing them to the same snake_case form the backend already expects, and ecommerce detail extraction now normalizes requested-field missingness before deciding on structured-data early exit so browser-expanded/custom section labels like `Product Story` keep the DOM tier live instead of disappearing behind a premature structured-data success. Focused verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_crawl_engine.py tests/services/test_browser_expansion_runtime.py -q` and `npm test -- components/crawl/crawl-config-screen.test.ts`; a full backend `pytest tests -q` run only hit the existing unrelated LOC-budget guard in `tests/services/test_structure.py` for `app/services/acquisition/browser_page_flow.py` and `app/services/field_value_dom.py`.
  - 2026-04-22 additional-fields exact-match follow-up corrected the slice-7 regression: the frontend now preserves raw additional-field labels instead of rewriting them before dispatch, detail extraction keeps exact requested section labels in play before broader alias collapse, and browser detail expansion now skips itself when those requested sections are already extractable from the current DOM. Focused verification passed with `pytest tests/services/test_field_policy.py tests/services/test_detail_extractor_structured_sources.py tests/services/test_browser_expansion_runtime.py -q` and `npm test -- components/crawl/crawl-config-screen.test.ts`; live PUMA repro on `https://in.puma.com/in/en/pd/deviate-nitro-elite-4-run-club-mens-road-running-shoes/312907?swatch=01` now returns `features_benefits` from the raw `Features & Benefits` request without mutating the page through unrelated accordion clicks.
  - 2026-04-22 failure-mode remediation slices 8-11 stayed in the existing extraction/browser/pipeline owners: detail variant recovery is now quality-gated instead of presence-gated, image materialization uses one shared canonical dedupe/filter path across JS state and DOM, browser-time requested-content extractability reuses the extractor’s DOM contract, and batch fan-out now preserves raw requested labels into acquisition/extraction dispatch. Focused verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_shared_variant_logic.py tests/services/test_crawl_engine.py -q --basetemp=.pytest-tmp-codex-1`, `pytest tests/services/test_browser_expansion_runtime.py tests/services/test_field_value_dom.py tests/services/test_structure.py -q --basetemp=.pytest-tmp-codex-6` except for the known unrelated LOC-budget blockers in `app/services/field_value_dom.py` and `app/services/extract/listing_visual.py`, and `pytest tests/services/test_field_policy.py tests/services/test_crawl_service.py tests/services/test_batch_runtime.py -q --basetemp=.pytest-tmp-codex-3`. The remaining gate on `tests/services/test_pipeline_core.py` is still blocked by a pre-existing Windows `pytest` temp-path cleanup failure (`PermissionError: [WinError 5] Access is denied` under `.pytest-tmp-codex-*`) that obscures the same three `tmp_path`-backed test cases as before this slice.
- 2026-04-22 audit follow-up stayed in the existing extraction/config owners: ecommerce-detail DOM completion now treats `additional_images` as a high-value DOM-backed field so structured-data early exit no longer suppresses gallery recovery when only a primary image is present, DOM variant fallback now threads the real `page_url` into `select_variant()`, and requested-field alias lookup keeps normalized requested labels as identity mappings alongside exact requested keys. Focused verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py tests/services/test_crawl_engine.py tests/services/test_field_policy.py -q`.
- 2026-04-22 review-comment remediation validated 11 new review findings sequentially against the live tree: five were real and fixed upstream (`browser_detail` requested-expander filtering, `field_value_dom` empty DOM-pattern extractability, `confidence` requested-count reporting, `crawl_crud` requested-field snapshot stamping, and `detail_extractor` site-shell rejection of structured detail pages). Six comments were confirmed incorrect for the current code path (`browser_page_flow` markdown noise tokening, browser-page-flow expansion skip semantics after extractability verification, traversal same-count pagination capture, traversal next-page recovery lookup, mixed-case fetch recovery mode handling, and the current DOM-completion / variant-axis guards). Focused verification passed with `pytest tests/services/test_browser_expansion_runtime.py tests/services/test_confidence.py tests/services/test_crawl_service.py tests/services/test_crawl_engine.py -q`.
- 2026-04-22 review-comment follow-up validated nine more comments sequentially against the live tree: four were real and fixed upstream (`telemetry` optional-`structlog` initialization, `browser_identity` iterable-setting normalization, `browser_identity` tuple-like brand handling for coherent client hints, and traversal heuristic over-counting of non-card support sections), while five were confirmed incorrect for the current code path (`celery_app` fallback config divergence, `browser_capture` summary-contract churn, timeout outcome handling in `read_network_payload_body()`, AOM expansion diagnostics state loss, and listing readiness reverting to visible-text-only fast paths). Focused verification passed with `pytest tests/services/test_browser_context.py tests/services/test_traversal_runtime.py -q` and `pytest tests/services/test_crawl_fetch_runtime.py -q -k "create_browser_identity_builds_generator_lazily or read_network_payload_body_maps_read_timeouts_to_timeout"`.
- 2026-04-22 slice-20 stayed in the acquisition/detail owners instead of adding downstream cleanup: browser detail expansion now skips plain navigation anchors with real `href`s so PDP acquisition no longer wanders into footer utility routes such as `/faqs` or `/careers`; detail extraction now carries the originally requested PDP identity through same-site utility redirects only when the extracted product still matches that requested slug, otherwise it drops the poisoned row; and DOM variant recovery now recognizes radio-based size/color controls and stock labels (`0 Left`, `17 Left`) so Souled Store-style size selectors populate `variant_axes`, `variants`, and selected availability. Focused verification passed with `pytest tests/services/test_browser_expansion_runtime.py tests/services/test_crawl_engine.py tests/services/test_detail_extractor_structured_sources.py -q`; targeted run-28 artifact replay now drops the poisoned `/faqs` row, salvages the `/mywishlist` row back onto the requested PDP URL, and saved/live Souled Store PDP repros now emit 8 size variants with stock-backed availability.
- Zara findings from the current code path:
  - saved listing artifact returns only 5 structured rows;
  - live browser listing run returns polluted output because traversal never recognizes Zara product tiles as listing cards, then extraction admits category-cloud rows;
  - saved detail artifact duplicates the same color axis and ingests the `4493/144/800` copy-code as an option value.
- Negative LOC is a real constraint here: when two paths do the same recovery/filtering work, the fix should pick one owner and delete the loser.


Acceptance-Led Recovery Remediation Plan
Summary
Replace today’s overlapping recovery/remediation plans with one architecture-first plan whose source of truth is the saved acceptance artifact at backend/artifacts/test_sites_acceptance/20260421T094742Z__full_pipeline__test_sites_tail.json, not prior “DONE” status in plan docs. The implementation goal is negative or flat backend LOC by deleting duplicate or inert recovery paths while fixing the failure classes that are still demonstrably live: premature browser give-up, empty/garbage listing extraction, missing diagnostics on runtime failure, detail variant/export leakage, and acceptance-critical surface misclassification.

This plan explicitly includes Zara and the other current failure modes. Anti-bot work stays generic and bounded: no provider-specific bypass design, but KitchenAid-class failures are in scope when the current runtime is giving up too early on content that should be recoverable.

Implementation Changes
1. Collapse overlapping recovery work into one audited execution stream
Treat these plans as superseded implementation claims, not current truth: old-app-recovery-features-plan.md, extraction-regression-remediation-plan.md, extraction-architecture-debt-remediation-plan.md, and the current Zara-only plan.
Use the acceptance artifact plus saved run artifacts as the regression corpus for all slices.
Add a short doc note in the replacement plan that “plan completion” only counts if the named acceptance regressions move, not if unit tests alone pass.
2. Fix acquisition robustness and browser give-up rules in their existing owners
Audit the actual path from crawl_fetch_runtime / browser runtime / traversal / browser page flow and delete any recovery branches that do not affect the final acquisition result.
Make browser failure paths always emit populated browser_diagnostics and a concrete stop reason; Reverb-style empty-diagnostics failure is a regression to eliminate.
Tighten listing-page readiness/traversal so Zara-like product grids are counted as listing evidence before recovery gives up.
Keep challenge handling generic and bounded:
preserve challenge detection,
add wait/retry/pacing behavior only where it improves recoverable cases,
include KitchenAid in the acceptance target,
do not expand this pass into proxy-rotation or provider-specific bypass logic.
3. Fix garbage listing output upstream instead of adding downstream cleanup
Make the listing extraction path choose a single clean owner for candidate selection and remove duplicated ranking/filtering behavior that lets SEO clouds, nav links, promo tiles, and utility links compete with product rows.
Cover the current acceptance garbage cases, not just Zara:
Zara category cloud pollution,
USAJOBS and Startup.jobs UI-chrome/job-routing garbage,
Karen Millen promo-banner titles,
Zadig & Voltaire low-signal listing rows,
GovPlanet listing-detection miss.
If a site is failing because surface routing is wrong before extraction starts, fix the routing owner (backend/harness_support.py for acceptance runs, plus any shared owner if the same misclassification exists in the app path) in the same pass.
4. Repair detail extraction correctness where current “ported” logic is still leaking junk
Fix Zara detail variant parsing in the current generic owner so duplicate axis groups and copy-code buttons cannot become option values.
Keep variants and selected_variant as real record fields in persisted data and exports; remove only non-user-facing filler/noise, not the intended variant payload.
Ensure markdown/page-context internals remain internal and do not leak into JSON/CSV record exports.
Audit whether any current variant logic is incorrectly biased toward Shopify-style data; if so, remove that assumption at the shared variant/detail owner instead of adding another fallback layer.
5. Keep scope architecture-first and deletion-first
Prefer deleting duplicate helpers/branches over adding new ones.
Do not add new adapters unless the acceptance failure is proven impossible to fix in the generic path.
Do not add publish/persistence compensation for upstream extraction/runtime defects.
Do not add new plan docs after this; use this as the single execution plan and mark the overlapping ones as superseded in-place.
Public Interfaces / Contract Changes
No API route or frontend contract changes are planned.
Internal contract tightening:
browser/runtime failures must produce structured browser_diagnostics rather than {},
acceptance harness surface inference is allowed to change for acceptance-critical URLs,
exported JSON/CSV record payloads remain user-facing only and must not include internal markdown/page-context filler.
Test Plan
Regression tests from saved artifacts:
Zara listing saved artifact returns only product rows and reaches the expected count for the targeted crawl path.
Zara detail saved artifact emits clean color variants without duplicate axes or copy-code pollution.
KitchenAid recoverable acquisition path does not fail via premature give-up.
Reverb-style runtime failure path records non-empty diagnostics.
Acceptance-critical routing/output tests:
USAJOBS routes to job listing and does not emit sort/filter chrome as title.
Startup.jobs routes to job listing and does not emit button text as title.
Karen Millen and Zadig & Voltaire reject banner/promo/low-signal rows.
GovPlanet no longer lands in listing_detection_failed from the current non-standard listing DOM.
Verification gates:
focused pytest subsets for traversal, browser runtime, listing extraction, detail extraction, record export, and harness support;
then rerun the saved 20-site acceptance tail and compare failure-mode counts against the current baseline.
Assumptions
Primary success bar is architecture-first, but acceptance movement is still the only valid proof that the architecture cleanup worked.
Anti-bot scope is limited to generic bounded recovery and better robustness/diagnostics; broad proxy or provider-specific bypass strategy is out of scope.
Acceptance-critical surface inference fixes are included in this pass because several current “successes” are false positives caused by wrong routing.
Negative LOC remains a hard constraint for implementation, even if the plan itself is longer than the earlier narrow Zara-only plan.
- 2026-04-22 performance follow-up stayed in the acquisition owner: `resolve_browser_readiness_policy()` now keeps the initial browser navigation at `domcontentloaded` even when later settling may still require `networkidle`, which removed Zara's repeated 20s `goto(..., wait_until=\"networkidle\")` fallback tax. Focused verification passed with `pytest tests/services/test_browser_expansion_runtime.py tests/services/test_platform_detection.py -q`, and live Zara timing moved from roughly `listing 30.6s acquire + 1.2s extract / detail 27.7s acquire + 1.3s extract` down to `listing 24.2s acquire + 0.3s extract / detail 4.8s acquire + 1.4s extract`; the remaining Zara listing cost is now dominated by traversal (`~18.4s`) rather than navigation.
- 2026-04-22 Stadium Goods listing audit stayed in the acquisition readiness owner instead of adding another site branch: `browser_readiness.listing_card_signal_count_impl()` and the mirrored `browser_runtime.listing_card_signal_count()` now reuse the shared heuristic card counter when configured selectors miss, so JS-heavy ecommerce listings no longer report `listing_card_count=0` just because platform-specific selectors are absent. Live manual repro on `https://www.stadiumgoods.com/collections/adidas-shoes` moved from `probe.is_ready=false` / `listing_card_count=0` to `probe.is_ready=true` / `listing_card_count=80` on the rendered DOM, and focused verification passed with `pytest tests/services/test_browser_expansion_runtime.py -q`.
- 2026-04-22 challenge-audit follow-up stayed in the harness/reporting owner instead of weakening crawler semantics: `harness_support.run_site_harness()` now emits a structured `challenge_summary` derived from `browser_diagnostics`, and `run_test_sites_acceptance.py` prints provider/evidence lines for blocked anti-bot runs. This keeps Stadium Goods-style `Verifying your connection...` / `429` cases visibly classified as acquisition blocking rather than looking like silent extraction empties. Focused verification passed with `pytest tests/test_harness_support.py -q`.
- 2026-04-22 Whirlpool/additional-fields follow-up stayed in shared extraction owners instead of site branches: camel/Pascal-case field normalization was fixed upstream in `field_policy.py`, network-payload signatures/list-container heuristics moved into `config/network_payload_specs.py`, requested extra fields now participate in payload anchor validation, and detail payloads with explicit mismatched URLs are rejected before they can overwrite the current page. Focused verification passed with `pytest tests/services/test_field_policy.py tests/services/test_network_payload_mapper.py tests/services/test_crawl_engine.py -q`. Live Whirlpool now restores the product title, canonical detail URL, `capacity`, `energy_rating`, and `additional_images`; remaining live pollution is limited to nested warranty/service option values still leaking into price/description selection.
- 2026-04-22 Souled Store accordion/markdown follow-up stayed in the shared DOM-section and markdown owners: anchor/hash accordion headers now resolve to their own panels instead of the shortest nearby collapse body, `Product Description` is treated as a canonical `description` alias upstream, and detail-page markdown no longer appends `Visible links:` when real body content already exists. Focused verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py -q`, `pytest tests/services/test_field_value_dom.py tests/services/test_browser_expansion_runtime.py -q`; replaying the saved Souled Store PDP artifact now promotes the full accordion `description` into `record.data` while keeping `product_details` confined to the material/care block instead of breadcrumb/link noise.
- 2026-04-22 HealthKart listing follow-up stayed in `listing_extractor.py` instead of adding downstream cleanup: standalone typed `Product` / `JobPosting` structured blobs on listing pages are now ignored unless the structured source shows actual listing evidence such as an `ItemList` or multiple typed items. This prevents brand/category pages from prepending one polluted structured row with page-level brand/description/rating/currency metadata ahead of real DOM product cards. Focused verification passed with `pytest tests/services/test_crawl_engine.py tests/services/test_selectolax_css_migration.py -q`, and replaying the saved HealthKart brand-page HTML now returns DOM-only product rows instead of the prior mixed structured+DOM output.
- 2026-04-22 generic variant-axis follow-up stayed in the shared variant owners instead of site branches: DOM variant recovery no longer hard-requires `size` / `color` labels before it will treat a select/radio/checkbox group as a real product option. The shared resolver now accepts plausible product-option labels generically, rejects Q&A/payment/shipping-style controls upstream, and normalizes common aliases such as `flavour -> flavor`. Focused verification passed with `pytest tests/services/test_detail_extractor_structured_sources.py -q -k "generic_dom_variant_axes_without_site_hardcoding or recovers_variant_axes_from_dom_controls_when_js_state_is_absent or recovers_radio_size_variants_with_stock_availability or skips_unnamed_dom_variant_groups or ignores_review_qa_controls_and_payment_icons or does_not_treat_question_radiogroup_as_size_variants"`. A broader pass over `test_detail_extractor_structured_sources.py`, `test_shared_variant_logic.py`, and `test_crawl_engine.py` only hit the pre-existing missing-artifact fixture `artifacts/runs/8/pages/dc80e38b20c25b9b.html`.
- 2026-04-22 review-comment cleanup validated the current batch sequentially against the live tree: six findings were real and fixed upstream (`TEST_SITES.md` commerce rows restored as table entries, `browser_identity` now preserves valid q-weighted `Accept-Language` headers, `cookie_store` cache clears now serialize on the shared lock and invalid run ids degrade to `None`, `browser_recovery` dropped its dead utility-anchor fallback, `traversal` now treats locator probe errors as inconclusive instead of dead, and shared variant-axis noise checks no longer reject labels on substring hits like `premium`). Four findings were confirmed stale for the current code path (`browser_detail` `locator.is_visible(timeout=...)` is supported by the repo's Playwright 1.58 dependency even though the timeout is ignored, `browser_runtime` forced-close-on-timeout does not currently break downstream diagnostics, and `pacing` host browser-first markers are already wired through `crawl_fetch_runtime`). Focused verification passed with `pytest tests/services/test_browser_context.py tests/services/test_crawl_fetch_runtime.py tests/services/test_traversal_runtime.py tests/services/test_shared_variant_logic.py -q`.
